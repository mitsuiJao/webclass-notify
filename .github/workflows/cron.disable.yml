name: Run index.js periodically
on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch: 

jobs:
  run-script:
    runs-on: ubuntu-latest
    environment: secret
    permissions:
      contents: read
      actions: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download state from previous run
        continue-on-error: true
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          LAST_RUN_ID=$(gh run list --workflow cron.yml --status success --limit 1 --json databaseId --jq '.[0].databaseId')
          if [ -n "$LAST_RUN_ID" ]; then
            echo "Found previous successful run: $LAST_RUN_ID"
            gh run download "$LAST_RUN_ID" -n notification-state -D . || echo "Artifact not found in run $LAST_RUN_ID"
          else
            echo "No successful previous run found"
          fi

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Run script
        run: node index.js
        env:
          USER_ID: ${{ secrets.USER_ID }}
          PASSWORD: ${{ secrets.PASSWORD }}
          TEAMS_WEBHOOK_URL: ${{ secrets.TEAMS_WEBHOOK_URL }}
          MFA_SECRET: ${{ secrets.MFA_SECRET }}

      - name: Upload state
        uses: actions/upload-artifact@v4
        with:
          name: notification-state
          path: notification_state.json
          retention-days: 3
          if-no-files-found: ignore
